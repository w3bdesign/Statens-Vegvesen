/**
 * Barrel re-export — each builder now lives in its own module under ./builders/.
 * Consumers can keep importing from this file without changes.
 */

export { buildVehicleData } from "./buildVehicleData";
